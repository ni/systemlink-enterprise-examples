# Bowen's notes

1. This node_modules currently handles building React app using Vite, Nimble and
   lint. Run `npm install` and then `npm run dev`to start the current app. Run
   `npm run lint` will run the lint in this project.
2. not so sure about the fonts yet. I don't remember where it mentioned what
   fonts to use.
3. The color about the ServiceHealthSummary I just picked whatever in the NI
   documents that are the closest to the Figma Design. I couldn't find one that
   fit exactly for the Avg Response.
4. Three Nimble components were used, NimbleButton, NimbleTable and
   NimbleTableColumnText.
5. Different section are indiviual component.
